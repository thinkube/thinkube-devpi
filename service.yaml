# Copyright Alejandro Martínez Corriá and the Thinkube contributors
# SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: Service
metadata:
  name: devpi
  namespace: devpi
spec:
  selector:
    app: devpi
  ports:
  - port: 3141
    targetPort: 3141
    protocol: TCP
